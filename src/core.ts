/**
 * `react-mentions-ts/core` — unstyled-by-default entry for apps without
 * Tailwind. Same components and API as the root entry; only the `unstyled`
 * default differs.
 */
export { MentionsInput, Mention } from './coreComponents'
export * from './sharedExports'
